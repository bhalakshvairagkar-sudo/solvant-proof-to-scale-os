services:
  - type: web
    name: solvant-proof-to-scale-os
    runtime: docker
    dockerfilePath: Dockerfile
    plan: free
    region: oregon
    envVars:
      - key: PORT
        value: 8000
      - key: GROQ_API_KEY
        sync: false
